steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/phishdetector', '.']
images:
  - 'gcr.io/$PROJECT_ID/phishdetector'
options:
  logging: CLOUD_LOGGING_ONLY
